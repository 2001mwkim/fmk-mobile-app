<?xml version="1.0" encoding="utf-8"?>
<!-- MY DRIVER 위젯(2x2 기본, 가로로 늘려도 같은 레이아웃). 데이터 키(myDriver*)는
     lib/services/fmk_home_widget_bridge.dart 의 _saveMyPicksPayload 와 수동 동기화.
     iOS 대응: ios/FmkWidgets/FmkMyPicksWidgets.swift. 기본 텍스트는 미리보기용 샘플 —
     Provider 가 런타임에 덮어쓴다. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root_my_driver"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg">

    <!-- 팀 컬러 글로우(우상단) -->
    <ImageView
        android:id="@+id/iv_myDriver_glow"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="-50dp"
        android:layout_marginEnd="-50dp"
        android:contentDescription="@null"
        android:src="@drawable/widget_glow" />

    <!-- 본문(설정됨) -->
    <LinearLayout
        android:id="@+id/grp_myDriver_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingStart="14dp"
        android:paddingEnd="12dp"
        android:paddingTop="11dp"
        android:paddingBottom="11dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">
            <TextView
                android:id="@+id/tv_myDriver_kicker"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:fontFamily="sans-serif-black"
                android:includeFontPadding="false"
                android:letterSpacing="0.16"
                android:maxLines="1"
                android:text="MY DRIVER"
                android:textColor="@color/fmk_red"
                android:textSize="9sp" />
            <TextView
                android:id="@+id/tv_myDriver_change"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-black"
                android:includeFontPadding="false"
                android:text=""
                android:textColor="@color/fmk_dim"
                android:textSize="10sp" />
        </LinearLayout>

        <!-- 가운데: 큰 약어 + 영문 이름 + 보조 줄 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="center_vertical"
            android:orientation="vertical">
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal">
                <!-- 팀 컬러는 약어 옆 얇은 바로만(순위 위젯 accent_bar 와 동일 문법) -->
                <ImageView
                    android:id="@+id/iv_myDriver_bar"
                    android:layout_width="3dp"
                    android:layout_height="24dp"
                    android:layout_marginEnd="8dp"
                    android:contentDescription="@null"
                    android:scaleType="fitXY"
                    android:src="@drawable/accent_bar" />
            <TextView
                android:id="@+id/tv_myDriver_code"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-black"
                android:includeFontPadding="false"
                android:letterSpacing="-0.03"
                android:maxLines="1"
                android:text="LEC"
                android:textColor="@color/fmk_white"
                android:textSize="34sp" />
            </LinearLayout>
            <TextView
                android:id="@+id/tv_myDriver_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:ellipsize="end"
                android:fontFamily="sans-serif-medium"
                android:includeFontPadding="false"
                android:maxLines="1"
                android:text="Charles Leclerc"
                android:textColor="@color/fmk_text"
                android:textSize="11sp"
                android:textStyle="bold" />
            <TextView
                android:id="@+id/tv_myDriver_sub"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:ellipsize="end"
                android:fontFamily="sans-serif-medium"
                android:includeFontPadding="false"
                android:maxLines="1"
                android:text="Ferrari"
                android:textColor="@color/fmk_dim"
                android:textSize="10sp"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- 하단: 왼쪽 큰 P4, 오른쪽 138 PTS (iOS FmkStatLine 과 동일) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="bottom"
            android:orientation="horizontal">
            <TextView
                android:id="@+id/tv_myDriver_pos"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:fontFamily="sans-serif-black"
                android:includeFontPadding="false"
                android:text="P4"
                android:textColor="@color/fmk_white"
                android:textSize="28sp" />
            <TextView
                android:id="@+id/tv_myDriver_pts"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="2dp"
                android:fontFamily="sans-serif-black"
                android:includeFontPadding="false"
                android:maxLines="1"
                android:text="138 PTS"
                android:textColor="@color/fmk_white"
                android:textSize="18sp" />
        </LinearLayout>
    </LinearLayout>

    <!-- 빈 상태(미설정) -->
    <LinearLayout
        android:id="@+id/grp_myDriver_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingStart="14dp"
        android:paddingEnd="12dp"
        android:paddingTop="11dp"
        android:paddingBottom="11dp"
        android:visibility="gone">
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-black"
            android:includeFontPadding="false"
            android:letterSpacing="0.16"
            android:text="MY DRIVER"
            android:textColor="@color/fmk_dim"
            android:textSize="9sp" />
        <TextView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="center_vertical"
            android:fontFamily="sans-serif-medium"
            android:includeFontPadding="false"
            android:lineSpacingExtra="2dp"
            android:text="내 드라이버\n탭해서 설정하면\n순위·포인트가 표시됩니다"
            android:textColor="@color/fmk_text"
            android:textSize="12sp"
            android:textStyle="bold" />
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:includeFontPadding="false"
            android:text="설정 › MY PICKS"
            android:textColor="@color/fmk_ghost"
            android:textSize="9sp"
            android:textStyle="bold" />
    </LinearLayout>
</FrameLayout>
